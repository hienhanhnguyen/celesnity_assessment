{
  "name": "@celesnity/fixtures",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Internal application API (JSON, paginated) + supplier crawler pages (HTML).",
  "main": "src/server.ts",
  "scripts": {
    "start": "node src/server.ts",
    "dev": "node --watch src/server.ts",
    "test": "node --test src/server.test.ts",
    "typecheck": "tsc -p tsconfig.json",
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "express": "5.2.1"
  },
  "devDependencies": {
    "@types/express": "5.0.6",
    "@types/node": "22.20.1"
  }
}
